telemetry:
  exporters:
    logging:
      stdout:
        enabled: true
        format: text

cors:
  allow_any_origin: true

headers:
  connector:
    # Set header configurations for all connectors
    all:
      request:
        - insert:
            name: "Authorization"
            value: "Bearer ${env.AUTHORIZATION}"